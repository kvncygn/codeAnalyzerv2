using System;

// Auto-generated helper file #1 (non-TCF methods only).
namespace Enterprise
{
    public static class Helpers1
    {
        // helper candidate H1_0
        /* small
           helper */
        public static int H1_0(int x)
        {
            int r = x + 0;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_1
        public static int H1_1(int x)
        {
            int r = x + 1;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_2
        /* small
           helper */
        public static int H1_2(int x)
        {
            int r = x + 2;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_3
        public static int H1_3(int x)
        {
            int r = x + 3;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_4
        public static int H1_4(int x)
        {
            int r = x + 4;      // inline comment
            return r;
        }

        // helper candidate H1_5
        public static int H1_5(int x)
        {
            int r = x + 5;      // inline comment
            return r;
        }

        // helper candidate H1_6
        public static int H1_6(int x)
        {
            int r = x + 6;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_7
        public static int H1_7(int x)
        {
            int r = x + 7;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_8
        public static int H1_8(int x)
        {
            int r = x + 8;      // inline comment
            return r;
        }

        // helper candidate H1_9
        /* small
           helper */
        public static int H1_9(int x)
        {
            int r = x + 9;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_10
        /* small
           helper */
        public static int H1_10(int x)
        {
            int r = x + 10;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_11
        public static int H1_11(int x)
        {
            int r = x + 11;      // inline comment
            return r;
        }

        // helper candidate H1_12
        /* small
           helper */
        public static int H1_12(int x)
        {
            int r = x + 12;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_13
        public static int H1_13(int x)
        {
            int r = x + 13;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_14
        public static int H1_14(int x)
        {
            int r = x + 14;      // inline comment
            return r;
        }

        // helper candidate H1_15
        public static int H1_15(int x)
        {
            int r = x + 15;      // inline comment
            return r;
        }

        // helper candidate H1_16
        public static int H1_16(int x)
        {
            int r = x + 16;      // inline comment
            return r;
        }

        // helper candidate H1_17
        /* small
           helper */
        public static int H1_17(int x)
        {
            int r = x + 17;      // inline comment
            return r;
        }

        // helper candidate H1_18
        public static int H1_18(int x)
        {
            int r = x + 18;      // inline comment
            return r;
        }

        // helper candidate H1_19
        public static int H1_19(int x)
        {
            int r = x + 19;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_20
        public static int H1_20(int x)
        {
            int r = x + 20;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_21
        public static int H1_21(int x)
        {
            int r = x + 21;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_22
        /* small
           helper */
        public static int H1_22(int x)
        {
            int r = x + 22;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_23
        /* small
           helper */
        public static int H1_23(int x)
        {
            int r = x + 23;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_24
        /* small
           helper */
        public static int H1_24(int x)
        {
            int r = x + 24;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_25
        /* small
           helper */
        public static int H1_25(int x)
        {
            int r = x + 25;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_26
        public static int H1_26(int x)
        {
            int r = x + 26;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_27
        public static int H1_27(int x)
        {
            int r = x + 27;      // inline comment
            return r;
        }

        // helper candidate H1_28
        /* small
           helper */
        public static int H1_28(int x)
        {
            int r = x + 28;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_29
        public static int H1_29(int x)
        {
            int r = x + 29;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_30
        /* small
           helper */
        public static int H1_30(int x)
        {
            int r = x + 30;      // inline comment
            return r;
        }

        // helper candidate H1_31
        public static int H1_31(int x)
        {
            int r = x + 31;      // inline comment
            return r;
        }

        // helper candidate H1_32
        public static int H1_32(int x)
        {
            int r = x + 32;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_33
        /* small
           helper */
        public static int H1_33(int x)
        {
            int r = x + 33;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_34
        /* small
           helper */
        public static int H1_34(int x)
        {
            int r = x + 34;      // inline comment
            return r;
        }

        // helper candidate H1_35
        /* small
           helper */
        public static int H1_35(int x)
        {
            int r = x + 35;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_36
        public static int H1_36(int x)
        {
            int r = x + 36;      // inline comment
            return r;
        }

        // helper candidate H1_37
        /* small
           helper */
        public static int H1_37(int x)
        {
            int r = x + 37;      // inline comment
            return r;
        }

        // helper candidate H1_38
        /* small
           helper */
        public static int H1_38(int x)
        {
            int r = x + 38;      // inline comment
            return r;
        }

        // helper candidate H1_39
        public static int H1_39(int x)
        {
            int r = x + 39;      // inline comment
            return r;
        }

        // helper candidate H1_40
        public static int H1_40(int x)
        {
            int r = x + 40;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_41
        public static int H1_41(int x)
        {
            int r = x + 41;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_42
        public static int H1_42(int x)
        {
            int r = x + 42;      // inline comment
            return r;
        }

        // helper candidate H1_43
        public static int H1_43(int x)
        {
            int r = x + 43;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H1_44
        /* small
           helper */
        public static int H1_44(int x)
        {
            int r = x + 44;      // inline comment
            return r;
        }

        // helper candidate H1_45
        public static int H1_45(int x)
        {
            int r = x + 45;      // inline comment
            return r;
        }

        // helper candidate H1_46
        public static int H1_46(int x)
        {
            int r = x + 46;      // inline comment
            return r;
        }

        // helper candidate H1_47
        public static int H1_47(int x)
        {
            int r = x + 47;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

    }
}
