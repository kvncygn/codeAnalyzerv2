using System;

// Auto-generated helper file #2 (non-TCF methods only).
namespace Enterprise
{
    public static class Helpers2
    {
        // helper candidate H2_0
        public static int H2_0(int x)
        {
            int r = x + 0;      // inline comment
            return r;
        }

        // helper candidate H2_1
        public static int H2_1(int x)
        {
            int r = x + 1;      // inline comment
            return r;
        }

        // helper candidate H2_2
        public static int H2_2(int x)
        {
            int r = x + 2;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_3
        public static int H2_3(int x)
        {
            int r = x + 3;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_4
        public static int H2_4(int x)
        {
            int r = x + 4;      // inline comment
            return r;
        }

        // helper candidate H2_5
        public static int H2_5(int x)
        {
            int r = x + 5;      // inline comment
            return r;
        }

        // helper candidate H2_6
        /* small
           helper */
        public static int H2_6(int x)
        {
            int r = x + 6;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_7
        public static int H2_7(int x)
        {
            int r = x + 7;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_8
        public static int H2_8(int x)
        {
            int r = x + 8;      // inline comment
            return r;
        }

        // helper candidate H2_9
        public static int H2_9(int x)
        {
            int r = x + 9;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_10
        public static int H2_10(int x)
        {
            int r = x + 10;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_11
        public static int H2_11(int x)
        {
            int r = x + 11;      // inline comment
            return r;
        }

        // helper candidate H2_12
        /* small
           helper */
        public static int H2_12(int x)
        {
            int r = x + 12;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_13
        public static int H2_13(int x)
        {
            int r = x + 13;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_14
        public static int H2_14(int x)
        {
            int r = x + 14;      // inline comment
            return r;
        }

        // helper candidate H2_15
        public static int H2_15(int x)
        {
            int r = x + 15;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_16
        public static int H2_16(int x)
        {
            int r = x + 16;      // inline comment
            return r;
        }

        // helper candidate H2_17
        public static int H2_17(int x)
        {
            int r = x + 17;      // inline comment
            return r;
        }

        // helper candidate H2_18
        /* small
           helper */
        public static int H2_18(int x)
        {
            int r = x + 18;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_19
        public static int H2_19(int x)
        {
            int r = x + 19;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_20
        public static int H2_20(int x)
        {
            int r = x + 20;      // inline comment
            return r;
        }

        // helper candidate H2_21
        public static int H2_21(int x)
        {
            int r = x + 21;      // inline comment
            return r;
        }

        // helper candidate H2_22
        public static int H2_22(int x)
        {
            int r = x + 22;      // inline comment
            return r;
        }

        // helper candidate H2_23
        public static int H2_23(int x)
        {
            int r = x + 23;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_24
        public static int H2_24(int x)
        {
            int r = x + 24;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_25
        public static int H2_25(int x)
        {
            int r = x + 25;      // inline comment
            return r;
        }

        // helper candidate H2_26
        /* small
           helper */
        public static int H2_26(int x)
        {
            int r = x + 26;      // inline comment
            return r;
        }

        // helper candidate H2_27
        /* small
           helper */
        public static int H2_27(int x)
        {
            int r = x + 27;      // inline comment
            return r;
        }

        // helper candidate H2_28
        /* small
           helper */
        public static int H2_28(int x)
        {
            int r = x + 28;      // inline comment
            return r;
        }

        // helper candidate H2_29
        /* small
           helper */
        public static int H2_29(int x)
        {
            int r = x + 29;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_30
        public static int H2_30(int x)
        {
            int r = x + 30;      // inline comment
            return r;
        }

        // helper candidate H2_31
        public static int H2_31(int x)
        {
            int r = x + 31;      // inline comment
            return r;
        }

        // helper candidate H2_32
        public static int H2_32(int x)
        {
            int r = x + 32;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_33
        public static int H2_33(int x)
        {
            int r = x + 33;      // inline comment
            return r;
        }

        // helper candidate H2_34
        public static int H2_34(int x)
        {
            int r = x + 34;      // inline comment
            return r;
        }

        // helper candidate H2_35
        public static int H2_35(int x)
        {
            int r = x + 35;      // inline comment
            return r;
        }

        // helper candidate H2_36
        public static int H2_36(int x)
        {
            int r = x + 36;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_37
        /* small
           helper */
        public static int H2_37(int x)
        {
            int r = x + 37;      // inline comment
            return r;
        }

        // helper candidate H2_38
        public static int H2_38(int x)
        {
            int r = x + 38;      // inline comment
            return r;
        }

        // helper candidate H2_39
        public static int H2_39(int x)
        {
            int r = x + 39;      // inline comment
            return r;
        }

        // helper candidate H2_40
        /* small
           helper */
        public static int H2_40(int x)
        {
            int r = x + 40;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_41
        public static int H2_41(int x)
        {
            int r = x + 41;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_42
        /* small
           helper */
        public static int H2_42(int x)
        {
            int r = x + 42;      // inline comment
            return r;
        }

        // helper candidate H2_43
        public static int H2_43(int x)
        {
            int r = x + 43;      // inline comment
            return r;
        }

        // helper candidate H2_44
        public static int H2_44(int x)
        {
            int r = x + 44;      // inline comment
            return r;
        }

        // helper candidate H2_45
        /* small
           helper */
        public static int H2_45(int x)
        {
            int r = x + 45;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_46
        /* small
           helper */
        public static int H2_46(int x)
        {
            int r = x + 46;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

        // helper candidate H2_47
        public static int H2_47(int x)
        {
            int r = x + 47;      // inline comment
            if (r < 0) { r = -r; }  // +1 complexity
            return r;
        }

    }
}
